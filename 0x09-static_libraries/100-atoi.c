#include "2-strlen.c"
#include "main.h"

/**
 * _atoi - I am not sure as I never did this bonus task
 * @s: I assume that is a pointer to a string
 *
 * Return: always 0 in my case
 */

int _atoi(char *s)
{
	return (_strlen(s));
}
